<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:background="@color/Primary"
    tools:context="com.mattias.achtungchat.RegistrationFragment">

    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:inputType="textEmailAddress"
        android:ems="10"
        android:id="@+id/editText_email_registration"
        android:textColor="@color/Accent"
        android:hint="@string/email_text_registration"
        android:textColorHint="@color/AccentBrighter" />

    <EditText
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:inputType="textPassword"
        android:ems="10"
        android:id="@+id/editText_password_registration"
        android:textColor="@color/Accent"
        android:hint="@string/password_text_registration"
        android:textColorHint="@color/AccentBrighter"
        android:layout_gravity="center_horizontal" />

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Save"
        android:textColor="@color/Accent"
        android:id="@+id/button_save"
        android:onClick="Registration_save"
        android:layout_gravity="center_horizontal"
        android:background="@color/White"/>

</LinearLayout>
